-- COCITESCRIPT | Ultimate Mobile Hub (No Fly)
if _G.CociteRunning then
    print("COCITESCRIPT уже запущен!")
    return
end
_G.CociteRunning = true

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local Settings = {
    Aimbot = {
        Enabled = false,
        LockedTarget = nil,
    },
    Noclip = {
        Enabled = false
    },
    ESP = {
        Enabled = false,
        Boxes = {},
        Names = {}
    }
}

-------------------------------------------------------------------
-- 1. ОКНО АВТОРИЗАЦИИ (ЧЕРНО-ФИОЛЕТОВЫЙ СТИЛЬ)
-------------------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CociteHubGui"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local AuthFrame = Instance.new("Frame")
AuthFrame.Parent = ScreenGui
AuthFrame.BackgroundColor3 = Color3.fromRGB(15, 12, 25)
AuthFrame.Position = UDim2.new(0.5, -140, 0.5, -85)
AuthFrame.Size = UDim2.new(0, 280, 0, 170)
AuthFrame.Active = true
AuthFrame.Draggable = true

local AuthCorner = Instance.new("UICorner")
AuthCorner.CornerRadius = UDim.new(0, 10)
AuthCorner.Parent = AuthFrame

local AuthStroke = Instance.new("UIStroke")
AuthStroke.Color = Color3.fromRGB(120, 40, 200)
AuthStroke.Thickness = 2
AuthStroke.Parent = AuthFrame

local AuthTitle = Instance.new("TextLabel")
AuthTitle.Parent = AuthFrame
AuthTitle.BackgroundTransparency = 1
AuthTitle.Size = UDim2.new(1, 0, 0, 40)
AuthTitle.Font = Enum.Font.GothamBold
AuthTitle.Text = "⚡ COCITESCRIPT LOGIN ⚡"
AuthTitle.TextColor3 = Color3.fromRGB(200, 150, 255)
AuthTitle.TextSize = 14

local TextBox = Instance.new("TextBox")
TextBox.Parent = AuthFrame
TextBox.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
TextBox.Position = UDim2.new(0, 20, 0, 50)
TextBox.Size = UDim2.new(0, 240, 0, 40)
TextBox.Font = Enum.Font.Gotham
TextBox.PlaceholderText = "Введите пароль..."
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextSize = 14

local TextCorner = Instance.new("UICorner")
TextCorner.CornerRadius = UDim.new(0, 8)
TextCorner.Parent = TextBox

local LoginBtn = Instance.new("TextButton")
LoginBtn.Parent = AuthFrame
LoginBtn.BackgroundColor3 = Color3.fromRGB(90, 20, 160)
LoginBtn.Position = UDim2.new(0, 20, 0, 105)
LoginBtn.Size = UDim2.new(0, 240, 0, 40)
LoginBtn.Font = Enum.Font.GothamBold
LoginBtn.Text = "ВОЙТИ"
LoginBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
LoginBtn.TextSize = 14

local LoginCorner = Instance.new("UICorner")
LoginCorner.CornerRadius = UDim.new(0, 8)
LoginCorner.Parent = LoginBtn

-------------------------------------------------------------------
-- 2. ГЛАВНОЕ МЕНЮ И КНОПКА ОТКРЫТИЯ
-------------------------------------------------------------------
local ToggleButton = Instance.new("ImageButton")
ToggleButton.Name = "CociteToggleBtn"
ToggleButton.Parent = ScreenGui
ToggleButton.BackgroundColor3 = Color3.fromRGB(20, 15, 30)
ToggleButton.Position = UDim2.new(0, 20, 0.3, 0)
ToggleButton.Size = UDim2.new(0, 50, 0, 50)
ToggleButton.Visible = false
ToggleButton.Active = true
ToggleButton.Draggable = true
ToggleButton.Image = "rbxassetid://6034287592"

local ToggleCorner = Instance.new("UICorner")
ToggleCorner.CornerRadius = UDim.new(1, 0)
ToggleCorner.Parent = ToggleButton

local ToggleStroke = Instance.new("UIStroke")
ToggleStroke.Color = Color3.fromRGB(140, 50, 230)
ToggleStroke.Thickness = 2
ToggleStroke.Parent = ToggleButton

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 12, 25)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -140)
MainFrame.Size = UDim2.new(0, 350, 0, 260) -- Уменьшена высота из-за удаления флая
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(120, 40, 200)
MainStroke.Thickness = 2
MainStroke.Parent = MainFrame

local Header = Instance.new("Frame")
Header.Parent = MainFrame
Header.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
Header.Size = UDim2.new(1, 0, 0, 40)

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 10)
HeaderCorner.Parent = Header

local Title = Instance.new("TextLabel")
Title.Parent = Header
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Size = UDim2.new(1, -15, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "⚡ COCITESCRIPT | HUB ⚡"
Title.TextColor3 = Color3.fromRGB(200, 150, 255)
Title.TextSize = 15
Title.TextXAlignment = Enum.TextXAlignment.Left

local CloseBtn = Instance.new("TextButton")
CloseBtn.Parent = Header
CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 40, 40)
CloseBtn.Position = UDim2.new(1, -35, 0.5, -12)
CloseBtn.Size = UDim2.new(0, 24, 0, 24)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 12
local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

ToggleButton.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
end)

LoginBtn.MouseButton1Click:Connect(function()
    if TextBox.Text == "123" then
        AuthFrame:Destroy()
        ToggleButton.Visible = true
        MainFrame.Visible = true
    else
        TextBox.Text = ""
        TextBox.PlaceholderText = "НЕВЕРНЫЙ ПАРОЛЬ!"
    end
end)

-------------------------------------------------------------------
-- 3. ПЛАВАЮЩАЯ КНОПКА АИМА
-------------------------------------------------------------------
local AimFloatBtn = Instance.new("TextButton")
AimFloatBtn.Name = "CociteAimButton"
AimFloatBtn.Parent = ScreenGui
AimFloatBtn.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
AimFloatBtn.Position = UDim2.new(0.8, 0, 0.4, 0)
AimFloatBtn.Size = UDim2.new(0, 95, 0, 45)
AimFloatBtn.Font = Enum.Font.GothamBold
AimFloatBtn.Text = "COCITE AIM"
AimFloatBtn.TextColor3 = Color3.fromRGB(200, 150, 255)
AimFloatBtn.TextSize = 12
AimFloatBtn.Visible = false
AimFloatBtn.Active = true
AimFloatBtn.Draggable = true

local AimFloatCorner = Instance.new("UICorner")
AimFloatCorner.CornerRadius = UDim.new(0, 8)
AimFloatCorner.Parent = AimFloatBtn

local AimFloatStroke = Instance.new("UIStroke")
AimFloatStroke.Color = Color3.fromRGB(140, 50, 230)
AimFloatStroke.Thickness = 2
AimFloatStroke.Parent = AimFloatBtn

local function GetClosestToCenter()
    local closestP = nil
    local shortestDist = math.huge
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local part = player.Character:FindFirstChild("Head")
            if part then
                local pos, onScreen = Camera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local magnitude = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                    if magnitude < shortestDist then
                        shortestDist = magnitude
                        closestP = player
                    end
                end
            end
        end
    end
    return closestP
end

local AimActive = false
AimFloatBtn.MouseButton1Click:Connect(function()
    AimActive = not AimActive
    if AimActive then
        AimFloatBtn.BackgroundColor3 = Color3.fromRGB(120, 40, 220)
        AimFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        Settings.Aimbot.LockedTarget = GetClosestToCenter()
    else
        AimFloatBtn.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
        AimFloatBtn.TextColor3 = Color3.fromRGB(200, 150, 255)
        Settings.Aimbot.LockedTarget = nil
    end
end)

RunService.RenderStepped:Connect(function()
    if Settings.Aimbot.Enabled and AimActive then
        local target = Settings.Aimbot.LockedTarget
        if target and target.Character and target.Character:FindFirstChild("Head") and target.Character.Humanoid.Health > 0 then
            local head = target.Character.Head
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, head.Position)
        else
            Settings.Aimbot.LockedTarget = GetClosestToCenter()
        end
    end
    
    if Settings.Noclip.Enabled and LocalPlayer.Character then
        for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

-------------------------------------------------------------------
-- 4. СОЗДАНИЕ КНОПОК МЕНЮ
-------------------------------------------------------------------
local function CreateButton(name, posY, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = MainFrame
    btn.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
    btn.Position = UDim2.new(0, 15, 0, posY)
    btn.Size = UDim2.new(0, 320, 0, 45)
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = name .. ": OFF"
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.TextSize = 13
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 8)
    corner.Parent = btn
    
    local stroke = Instance.new("UIStroke")
    stroke.Color = Color3.fromRGB(80, 30, 150)
    stroke.Thickness = 1
    stroke.Parent = btn
    
    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        if active then
            btn.BackgroundColor3 = Color3.fromRGB(110, 30, 200)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Text = name .. ": ON"
        else
            btn.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
            btn.TextColor3 = Color3.fromRGB(200, 200, 200)
            btn.Text = name .. ": OFF"
        end
        callback(active)
    end)
    return btn
end

-------------------------------------------------------------------
-- 5. ESP (BOXES + NAMES)
-------------------------------------------------------------------
local function ToggleESP(state)
    Settings.ESP.Enabled = state
    if not state then
        for _, box in pairs(Settings.ESP.Boxes) do box:Remove() end
        for _, name in pairs(Settings.ESP.Names) do name:Remove() end
        Settings.ESP.Boxes = {}
        Settings.ESP.Names = {}
    end
end

RunService.RenderStepped:Connect(function()
    if not Settings.ESP.Enabled then return end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local hrp = player.Character.HumanoidRootPart
            local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
            
            if not Settings.ESP.Boxes[player] then
                local box = Drawing.new("Square")
                box.Visible = false
                box.Color = Color3.fromRGB(180, 50, 255)
                box.Thickness = 1.5
                box.Filled = false
                Settings.ESP.Boxes[player] = box
            end
            
            if not Settings.ESP.Names[player] then
                local nameText = Drawing.new("Text")
                nameText.Visible = false
                nameText.Color = Color3.fromRGB(255, 255, 255)
                nameText.Size = 14
                nameText.Center = true
                nameText.Outline = true
                Settings.ESP.Names[player] = nameText
            end
            
            local box = Settings.ESP.Boxes[player]
            local nameText = Settings.ESP.Names[player]
            
            if onScreen then
                local size = Vector3.new(2, 3, 0) * (300 / pos.Z)
                box.Size = Vector2.new(size.X, size.Y)
                box.Position = Vector2.new(pos.X - size.X / 2, pos.Y - size.Y / 2)
                box.Visible = true
                
                nameText.Text = player.Name
                nameText.Position = Vector2.new(pos.X, pos.Y - size.Y / 2 - 18)
                nameText.Visible = true
            else
                box.Visible = false
                nameText.Visible = false
            end
        else
            if Settings.ESP.Boxes[player] then Settings.ESP.Boxes[player].Visible = false end
            if Settings.ESP.Names[player] then Settings.ESP.Names[player].Visible = false end
        end
    end
end)

-------------------------------------------------------------------
-- ДОБАВЛЕНИЕ КНОПОК В МЕНЮ
-------------------------------------------------------------------
CreateButton("Aimbot", 50, function(state) 
    Settings.Aimbot.Enabled = state
    AimFloatBtn.Visible = state
    if not state then 
        AimActive = false
        AimFloatBtn.BackgroundColor3 = Color3.fromRGB(25, 20, 35)
        AimFloatBtn.TextColor3 = Color3.fromRGB(200, 150, 255)
        Settings.Aimbot.LockedTarget = nil
    end
end)

CreateButton("ESP (Boxes + Names)", 105, function(state) ToggleESP(state) end)
CreateButton("Noclip", 160, function(state) Settings.Noclip.Enabled = state end)

print("COCITESCRIPT Hub успешно загружен!")

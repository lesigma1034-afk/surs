-- COCITESCRIPT | Secured Hub
if _G.CociteRunning then
    print("COCITESCRIPT уже запущен!")
    return
end
_G.CociteRunning = true

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local Settings = {
    Aimbot = {
        Enabled = false,
        Key = Enum.UserInputType.MouseButton2, -- ПКМ
        Smoothness = 0.15,
        Prediction = 0.035,
        HitPart = "HumanoidRootPart"
    },
    Fly = {
        Enabled = false,
        Speed = 50
    },
    Noclip = {
        Enabled = false
    }
}

-------------------------------------------------------------------
-- ОКНО АВТОРИЗАЦИИ (ПАРОЛЬ)
-------------------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CociteAuthGUI"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local AuthFrame = Instance.new("Frame")
AuthFrame.Name = "AuthFrame"
AuthFrame.Parent = ScreenGui
AuthFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
AuthFrame.Position = UDim2.new(0.5, -150, 0.5, -90)
AuthFrame.Size = UDim2.new(0, 300, 0, 180)
AuthFrame.Active = true
AuthFrame.Draggable = true

local AuthCorner = Instance.new("UICorner")
AuthCorner.CornerRadius = UDim.new(0, 8)
AuthCorner.Parent = AuthFrame

local AuthHeader = Instance.new("Frame")
AuthHeader.Parent = AuthFrame
AuthHeader.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
AuthHeader.Size = UDim2.new(1, 0, 0, 35)

local AuthHeaderCorner = Instance.new("UICorner")
AuthHeaderCorner.CornerRadius = UDim.new(0, 8)
AuthHeaderCorner.Parent = AuthHeader

local AuthTitle = Instance.new("TextLabel")
AuthTitle.Parent = AuthHeader
AuthTitle.BackgroundTransparency = 1
AuthTitle.Size = UDim2.new(1, 0, 1, 0)
AuthTitle.Font = Enum.Font.GothamBold
AuthTitle.Text = "⚡ COCITESCRIPT | LOGIN ⚡"
AuthTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
AuthTitle.TextSize = 14

local TextBox = Instance.new("TextBox")
TextBox.Parent = AuthFrame
TextBox.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
TextBox.Position = UDim2.new(0, 25, 0, 55)
TextBox.Size = UDim2.new(0, 250, 0, 40)
TextBox.Font = Enum.Font.Gotham
TextBox.PlaceholderText = "Введите пароль (например: 123)"
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextSize = 13

local TextCorner = Instance.new("UICorner")
TextCorner.CornerRadius = UDim.new(0, 6)
TextCorner.Parent = TextBox

local LoginBtn = Instance.new("TextButton")
LoginBtn.Parent = AuthFrame
LoginBtn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
LoginBtn.Position = UDim2.new(0, 25, 0, 110)
LoginBtn.Size = UDim2.new(0, 250, 0, 40)
LoginBtn.Font = Enum.Font.GothamBold
LoginBtn.Text = "ВОЙТИ"
LoginBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
LoginBtn.TextSize = 14

local LoginCorner = Instance.new("UICorner")
LoginCorner.CornerRadius = UDim.new(0, 6)
LoginCorner.Parent = LoginBtn

-------------------------------------------------------------------
-- ОСНОВНОЙ ИНТЕРФЕЙС (СКРЫТ ДО ВВОДА ПАРОЛЯ)
-------------------------------------------------------------------
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
MainFrame.Position = UDim2.new(0.5, -175, 0.5, -125)
MainFrame.Size = UDim2.new(0, 350, 0, 260)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = false -- Скрыто до ввода пароля

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

local Header = Instance.new("Frame")
Header.Parent = MainFrame
Header.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
Header.Size = UDim2.new(1, 0, 0, 40)

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 8)
HeaderCorner.Parent = Header

local Title = Instance.new("TextLabel")
Title.Parent = Header
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 15, 0, 0)
Title.Size = UDim2.new(1, -15, 1, 0)
Title.Font = Enum.Font.GothamBold
Title.Text = "⚡ COCITESCRIPT ⚡"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.TextXAlignment = Enum.TextXAlignment.Left

-- Проверка пароля при нажатии
LoginBtn.MouseButton1Click:Connect(function()
    if TextBox.Text == "123" then
        AuthFrame:Destroy() -- Закрываем окно пароля
        MainFrame.Visible = true -- Открываем главный чит
    else
        TextBox.Text = ""
        TextBox.PlaceholderText = "НЕВЕРНЫЙ ПАРОЛЬ!"
    end
end)

-------------------------------------------------------------------
-- ФУНКЦИИ КНОПОК
-------------------------------------------------------------------
local function CreateButton(name, posY, callback)
    local btn = Instance.new("TextButton")
    btn.Parent = MainFrame
    btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    btn.Position = UDim2.new(0, 15, 0, posY)
    btn.Size = UDim2.new(0, 320, 0, 45)
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = name .. ": OFF"
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.TextSize = 14
    
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, 6)
    corner.Parent = btn
    
    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        if active then
            btn.BackgroundColor3 = Color3.fromRGB(0, 170, 100)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Text = name .. ": ON"
        else
            btn.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
            btn.TextColor3 = Color3.fromRGB(200, 200, 200)
            btn.Text = name .. ": OFF"
        end
        callback(active)
    end)
    return btn
end

-- Логика Aimbot (Плавный, не кривой)
local function GetClosestPlayer()
    local closestPlayer = nil
    local shortestDist = math.huge
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local part = player.Character:FindFirstChild(Settings.Aimbot.HitPart)
            if part then
                local pos, onScreen = Camera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local magnitude = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                    if magnitude < shortestDist then
                        shortestDist = magnitude
                        closestPlayer = player
                    end
                end
            end
        end
    end
    return closestPlayer
end

local aimbotKeyPressed = false
UserInputService.InputBegan:Connect(function(input)
    if input.UserInputType == Settings.Aimbot.Key then aimbotKeyPressed = true end
end)
UserInputService.InputEnded:Connect(function(input)
    if input.UserInputType == Settings.Aimbot.Key then aimbotKeyPressed = false end
end)

RunService.RenderStepped:Connect(function()
    if Settings.Aimbot.Enabled and aimbotKeyPressed then
        local target = GetClosestPlayer()
        if target and target.Character and target.Character:FindFirstChild(Settings.Aimbot.HitPart) then
            local targetPart = target.Character[Settings.Aimbot.HitPart]
            local predictedPos = targetPart.Position + (targetPart.AssemblyLinearVelocity * Settings.Aimbot.Prediction)
            Camera.CFrame = Camera.CFrame:Lerp(CFrame.new(Camera.CFrame.Position, predictedPos), Settings.Aimbot.Smoothness)
        end
    end
    
    if Settings.Noclip.Enabled and LocalPlayer.Character then
        for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

-- Логика Fly
local flyConnection
local function ToggleFly(state)
    Settings.Fly.Enabled = state
    local character = LocalPlayer.Character
    if not character or not character:FindFirstChild("HumanoidRootPart") then return end
    local hrp = character.HumanoidRootPart
    local humanoid = character:FindFirstChildOfClass("Humanoid")
    
    if state then
        local bv = Instance.new("BodyVelocity")
        bv.Name = "CociteFlyVelocity"
        bv.Parent = hrp
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.Velocity = Vector3.new(0, 0, 0)
        
        local bg = Instance.new("BodyGyro")
        bg.Name = "CociteFlyGyro"
        bg.Parent = hrp
        bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
        
        flyConnection = RunService.RenderStepped:Connect(function()
            if not Settings.Fly.Enabled then return end
            if humanoid then humanoid.PlatformStand = true end
            local camCF = Camera.CFrame
            local moveDir = Vector3.new()
            if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + camCF.LookVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - camCF.LookVector end
            if UserIconService_or_UserInputService_Check and UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camCF.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir - camCF.RightVector end
            if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + camCF.RightVector end
            bv.Velocity = moveDir * Settings.Fly.Speed
            bg.CFrame = camCF
        end)
    else
        if flyConnection then flyConnection:Disconnect() end
        if hrp:FindFirstChild("CociteFlyVelocity") then hrp.CociteFlyVelocity:Destroy() end
        if hrp:FindFirstChild("CociteFlyGyro") then hrp.CociteFlyGyro:Destroy() end
        if humanoid then humanoid.PlatformStand = false end
    end
end

-- Кнопки интерфейса
CreateButton("Aimbot (Hold Right Click)", 55, function(state) Settings.Aimbot.Enabled = state end)
CreateButton("Custom Fly (WASD)", 110, function(state) ToggleFly(state) end)
CreateButton("Noclip", 165, function(state) Settings.Noclip.Enabled = state end)

print("COCITESCRIPT успешно загружен! Введите пароль 123 для доступа.")

-- COCITESCRIPT | Fixed & Clean Hub
if _G.CociteRunning then
    print("COCITESCRIPT уже запущен!")
    return
end
_G.CociteRunning = true

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

local Settings = {
    Aimbot = {
        Enabled = false,
        LockedTarget = nil,
    },
    BulletTP = {
        Enabled = false
    },
    Noclip = {
        Enabled = false
    },
    ESP = {
        Enabled = false,
        Boxes = {},
        Names = {}
    }
}

-------------------------------------------------------------------
-- 1. ОКНО АВТОРИЗАЦИИ
-------------------------------------------------------------------
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CociteHubGui"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local AuthFrame = Instance.new("Frame", ScreenGui)
AuthFrame.BackgroundColor3 = Color3.fromRGB(10, 8, 15)
AuthFrame.Position = UDim2.new(0.5, -140, 0.5, -85)
AuthFrame.Size = UDim2.new(0, 280, 0, 170)
AuthFrame.Active = true
AuthFrame.Draggable = true
Instance.new("UICorner", AuthFrame).CornerRadius = UDim.new(0, 10)

local AuthStroke = Instance.new("UIStroke", AuthFrame)
AuthStroke.Color = Color3.fromRGB(100, 30, 180)
AuthStroke.Thickness = 2

local AuthTitle = Instance.new("TextLabel", AuthFrame)
AuthTitle.BackgroundTransparency = 1
AuthTitle.Size = UDim2.new(1, 0, 0, 40)
AuthTitle.Font = Enum.Font.GothamBold
AuthTitle.Text = "⚡ COCITESCRIPT LOGIN ⚡"
AuthTitle.TextColor3 = Color3.fromRGB(180, 130, 255)
AuthTitle.TextSize, AuthTitle.TextXAlignment = 13, Enum.TextXAlignment.Center

local TextBox = Instance.new("TextBox", AuthFrame)
TextBox.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
TextBox.Position = UDim2.new(0, 20, 0, 50)
TextBox.Size = UDim2.new(0, 240, 0, 40)
TextBox.Font = Enum.Font.Gotham
TextBox.PlaceholderText = "Пароль (123)..."
TextBox.Text = ""
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextSize = 13
Instance.new("UICorner", TextBox).CornerRadius = UDim.new(0, 6)

local LoginBtn = Instance.new("TextButton", AuthFrame)
LoginBtn.BackgroundColor3 = Color3.fromRGB(80, 20, 140)
LoginBtn.Position = UDim2.new(0, 20, 0, 105)
LoginBtn.Size = UDim2.new(0, 240, 0, 40)
LoginBtn.Font = Enum.Font.GothamBold
LoginBtn.Text = "ВОЙТИ"
LoginBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
LoginBtn.TextSize = 13
Instance.new("UICorner", LoginBtn).CornerRadius = UDim.new(0, 6)

-------------------------------------------------------------------
-- 2. НОВОЕ СТИЛЬНОЕ МЕНЮ (ДРУГОЙ ДИЗАЙН)
-------------------------------------------------------------------
local ToggleButton = Instance.new("ImageButton", ScreenGui)
ToggleButton.Name = "CociteToggleBtn"
ToggleButton.BackgroundColor3 = Color3.fromRGB(15, 12, 22)
ToggleButton.Position = UDim2.new(0, 20, 0.3, 0)
ToggleButton.Size = UDim2.new(0, 46, 0, 46)
ToggleButton.Visible = false
ToggleButton.Active = true
ToggleButton.Draggable = true
ToggleButton.Image = "rbxassetid://6034287592"
Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1, 0)

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Name = "MainFrame"
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 8, 15)
MainFrame.Position = UDim2.new(0.5, -160, 0.5, -150)
MainFrame.Size = UDim2.new(0, 320, 0, 290)
MainFrame.Visible = false
MainFrame.Active = true
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Color = Color3.fromRGB(100, 30, 180)
MainStroke.Thickness = 2

-- Верхняя панелька
local TopBar = Instance.new("Frame", MainFrame)
TopBar.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
TopBar.Size = UDim2.new(1, 0, 0, 38)
Instance.new("UICorner", TopBar).CornerRadius = UDim.new(0, 12)

local HubTitle = Instance.new("TextLabel", TopBar)
HubTitle.BackgroundTransparency = 1
HubTitle.Position = UDim2.new(0, 12, 0, 0)
HubTitle.Size = UDim2.new(1, -12, 1, 0)
HubTitle.Font = Enum.Font.GothamBold
HubTitle.Text = "⚡ COCITE HUB (NEW) ⚡"
HubTitle.TextColor3 = Color3.fromRGB(180, 130, 255)
HubTitle.TextSize = 13
HubTitle.TextXAlignment = Enum.TextXAlignment.Left

local CloseBtn = Instance.new("TextButton", TopBar)
CloseBtn.BackgroundColor3 = Color3.fromRGB(160, 30, 30)
CloseBtn.Position = UDim2.new(1, -32, 0.5, -11)
CloseBtn.Size = UDim2.new(0, 22, 0, 22)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 11
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 5)

CloseBtn.MouseButton1Click:Connect(function() MainFrame.Visible = false end)
ToggleButton.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)

LoginBtn.MouseButton1Click:Connect(function()
    if TextBox.Text == "123" then
        AuthFrame:Destroy()
        ToggleButton.Visible = true
        MainFrame.Visible = true
    else
        TextBox.Text = ""
        TextBox.PlaceholderText = "НЕВЕРНЫЙ ПАРОЛЬ!"
    end
end)

-------------------------------------------------------------------
-- 3. ПЛАВАЮЩАЯ КНОПКА АИМА
-------------------------------------------------------------------
local AimFloatBtn = Instance.new("TextButton", ScreenGui)
AimFloatBtn.Name = "CociteAimButton"
AimFloatBtn.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
AimFloatBtn.Position = UDim2.new(0.8, 0, 0.4, 0)
AimFloatBtn.Size = UDim2.new(0, 90, 0, 40)
AimFloatBtn.Font = Enum.Font.GothamBold
AimFloatBtn.Text = "AIM LOCK"
AimFloatBtn.TextColor3 = Color3.fromRGB(180, 130, 255)
AimFloatBtn.TextSize = 11
AimFloatBtn.Visible = false
AimFloatBtn.Active = true
AimFloatBtn.Draggable = true
Instance.new("UICorner", AimFloatBtn).CornerRadius = UDim.new(0, 6)

local function GetClosestTarget()
    local closestP = nil
    local shortestDist = math.huge
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local part = player.Character:FindFirstChild("Head")
            if part then
                local pos, onScreen = Camera:WorldToViewportPoint(part.Position)
                if onScreen then
                    local magnitude = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)).Magnitude
                    if magnitude < shortestDist then
                        shortestDist = magnitude
                        closestP = player
                    end
                end
            end
        end
    end
    return closestP
end

local AimActive = false
AimFloatBtn.MouseButton1Click:Connect(function()
    AimActive = not AimActive
    if AimActive then
        AimFloatBtn.BackgroundColor3 = Color3.fromRGB(100, 25, 180)
        AimFloatBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        Settings.Aimbot.LockedTarget = GetClosestTarget()
    else
        AimFloatBtn.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
        AimFloatBtn.TextColor3 = Color3.fromRGB(180, 130, 255)
        Settings.Aimbot.LockedTarget = nil
    end
end)

-------------------------------------------------------------------
-- 4. ЛОГИКА ФУНКЦИЙ
-------------------------------------------------------------------
RunService.RenderStepped:Connect(function()
    if Settings.Aimbot.Enabled and AimActive then
        local target = Settings.Aimbot.LockedTarget
        if target and target.Character and target.Character:FindFirstChild("Head") and target.Character.Humanoid.Health > 0 then
            Camera.CFrame = CFrame.new(Camera.CFrame.Position, target.Character.Head.Position)
        else
            Settings.Aimbot.LockedTarget = GetClosestTarget()
        end
    end
    
    -- Безопасный Bullet TP к ближайшему врагу
    if Settings.BulletTP.Enabled then
        pcall(function()
            local target = GetClosestTarget()
            if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
                local hrp = target.Character.HumanoidRootPart
                for _, v in pairs(workspace:GetChildren()) do
                    if v.Name == "Handle" and v:IsA("BasePart") then
                        v.CFrame = hrp.CFrame
                    end
                end
            end
        end)
    end
    
    if Settings.Noclip.Enabled and LocalPlayer.Character then
        for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do
            if part:IsA("BasePart") then part.CanCollide = false end
        end
    end
end)

-------------------------------------------------------------------
-- 5. КНОПКИ В МЕНЮ
-------------------------------------------------------------------
local function CreateButton(name, posY, callback)
    local btn = Instance.new("TextButton", MainFrame)
    btn.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
    btn.Position = UDim2.new(0, 15, 0, posY)
    btn.Size = UDim2.new(0, 290, 0, 42)
    btn.Font = Enum.Font.GothamSemibold
    btn.Text = name .. ": OFF"
    btn.TextColor3 = Color3.fromRGB(170, 170, 170)
    btn.TextSize = 12
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    
    local active = false
    btn.MouseButton1Click:Connect(function()
        active = not active
        if active then
            btn.BackgroundColor3 = Color3.fromRGB(90, 20, 160)
            btn.TextColor3 = Color3.fromRGB(255, 255, 255)
            btn.Text = name .. ": ON"
        else
            btn.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
            btn.TextColor3 = Color3.fromRGB(170, 170, 170)
            btn.Text = name .. ": OFF"
        end
        callback(active)
    end)
    return btn
end

-------------------------------------------------------------------
-- 6. ESP (BOXES + NAMES)
-------------------------------------------------------------------
local function ToggleESP(state)
    Settings.ESP.Enabled = state
    if not state then
        for _, box in pairs(Settings.ESP.Boxes) do box:Remove() end
        for _, name in pairs(Settings.ESP.Names) do name:Remove() end
        Settings.ESP.Boxes = {}
        Settings.ESP.Names = {}
    end
end

RunService.RenderStepped:Connect(function()
    if not Settings.ESP.Enabled then return end
    for _, player in ipairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("HumanoidRootPart") and player.Character:FindFirstChild("Humanoid") and player.Character.Humanoid.Health > 0 then
            local hrp = player.Character.HumanoidRootPart
            local pos, onScreen = Camera:WorldToViewportPoint(hrp.Position)
            
            if not Settings.ESP.Boxes[player] then
                local box = Drawing.new("Square")
                box.Visible = false
                box.Color = Color3.fromRGB(160, 40, 240)
                box.Thickness = 1.5
                box.Filled = false
                Settings.ESP.Boxes[player] = box
            end
            
            if not Settings.ESP.Names[player] then
                local nameText = Drawing.new("Text")
                nameText.Visible = false
                nameText.Color = Color3.fromRGB(255, 255, 255)
                nameText.Size = 12
                nameText.Center = true
                nameText.Outline = true
                Settings.ESP.Names[player] = nameText
            end
            
            local box = Settings.ESP.Boxes[player]
            local nameText = Settings.ESP.Names[player]
            
            if onScreen then
                local size = Vector3.new(2, 3, 0) * (300 / pos.Z)
                box.Size = Vector2.new(size.X, size.Y)
                box.Position = Vector2.new(pos.X - size.X / 2, pos.Y - size.Y / 2)
                box.Visible = true
                
                nameText.Text = player.Name
                nameText.Position = Vector2.new(pos.X, pos.Y - size.Y / 2 - 16)
                nameText.Visible = true
            else
                box.Visible = false
                nameText.Visible = false
            end
        else
            if Settings.ESP.Boxes[player] then Settings.ESP.Boxes[player].Visible = false end
            if Settings.ESP.Names[player] then Settings.ESP.Names[player].Visible = false end
        end
    end
end)

-------------------------------------------------------------------
-- ИНИЦИАЛИЗАЦИЯ КНОПОК
-------------------------------------------------------------------
CreateButton("Aimbot", 50, function(state) 
    Settings.Aimbot.Enabled = state
    AimFloatBtn.Visible = state
    if not state then 
        AimActive = false
        AimFloatBtn.BackgroundColor3 = Color3.fromRGB(18, 14, 25)
        AimFloatBtn.TextColor3 = Color3.fromRGB(180, 130, 255)
        Settings.Aimbot.LockedTarget = nil
    end
end)

CreateButton("Bullet TP", 100, function(state) Settings.BulletTP.Enabled = state end)
CreateButton("ESP (Boxes + Names)", 150, function(state) ToggleESP(state) end)
CreateButton("Noclip", 200, function(state) Settings.Noclip.Enabled = state end)

print("Cocite Hub загружен без палева!")
